% example.tex — minimal smoke-test document.
% It \input{}s the SAME preamble you will actually use (assets/preamble.tex),
% so compiling this verifies the real environment — not a reduced copy.
% Exercises every edge that hides bugs: named operators + Latin-in-math (the
% [no-math] bug), a lone Latin word inside Hebrew (the fallback), braket macros
% vs. quantikz, a hyperref bookmark with math (\texorpdfstring), a colored box,
% and a SWAP circuit in the LTR island.
%
% Build:  bash scripts/build.sh example.tex      (run from a dir holding preamble.tex)
\input{preamble.tex}
\renewcommand{\thesection}{\arabic{section}}  % report class, but this demo has no \chapter

\begin{document}

\begin{center}\Large\textbf{מסמך לדוגמה — עברית עם מתמטיקה}\end{center}
\foreignlanguage{english}{N. D. Mermin, ``Quantum Computer Science'' (2007)}

% מתמטיקה בכותרת: הארגומנט השני (לבוקמרק) חייב להיות נקי מ-^ _ $ פעילים
\section{אומדן פאזה ל-\texorpdfstring{$n$}{n} סיביות ושער
  \texorpdfstring{$c^n$-$Z$}{c\textasciicircum n-Z}}

\noindent
נתון אוסצילטור עם $H_0 = \hbar\omega(a^\dagger a + \tfrac12)$, ומצב qubit כללי.
אופרטורים: $\sin\theta$, $\cos\theta$, $\Tr(\dyad{\psi})=1$ — אם יוצאים tofu, חסר \verb|[no-math]|.

\[ i\hbar\frac{\partial}{\partial t}\ket{\psi} = H\ket{\psi},\qquad
   \sigma_y = \begin{pmatrix} 0 & -i \\ i & 0 \end{pmatrix}. \]

\begin{defbox}[frametitle={הגדרה — שער הדמרד}]
$H=\tfrac1{\sqrt2}(X+Z)$, ומקיים $HXH=Z$. לטינית במתמטיקה: $\det(H)=-1$.
\end{defbox}

\noindent אופרטור הזהות נכתב $\id$, ומצב מעורב כללי הוא
$\rho=\half(\id+\vec a\cdot\vec\sigma)$ — אם במקום $\id$ מופיע סימן turnstile, מישהו
שבר את ה-macro \verb|\id| ב-preamble.

\noindent\textbf{מעגל (זוג בל) עם SWAP:}
\begin{qcirc}
\begin{quantikz}
\lstick{$\ket0$} & \gate{H} & \ctrl{1} & \swap{1} & \meter{} \\
\lstick{$\ket0$} & \qw      & \targ{}  & \targX{} & \qw
\end{quantikz}
\end{qcirc}

\begin{itemize}
\item[(\textbf{א})] סעיף ראשון, עם $\langle x\rangle=0$.
\item[(\textbf{ב})] סעיף שני: the energy is $E_n=\hbar\omega(n+\tfrac12)$.
\end{itemize}

% v3.8 — text INSIDE math: Hebrew label under a brace, a Latin name (never bare
% \en), Latin-WITH-parens (\en forces LTR so the parens do not mirror), and a
% function on a Hebrew argument (parens at math level). If a paren here mirrors
% ("(TNR(") or a label tofus, the v3.8 math-text rule regressed.
\noindent\textbf{\h{טקסט בתוך מתמטיקה (גרסה 3.8):}}
\[ X \sim \text{\en{Normal}}(\mu,\sigma^2), \qquad
   \underbrace{mc^2}_{\text{\h{אנרגיית מנוחה}}} \]
\[ \text{\en{Specificity (TNR)}} = \frac{\text{TN}}{\text{TN}+\text{FP}}, \qquad
   \text{\en{Entropy}}(\text{\h{קבוצה}}) \]

\end{document}
